name: Build Windows EXE

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Clonar os arquivos
      uses: actions/checkout@v4
      
    - name: Configurar o Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Instalar dependencias e PyInstaller
      # ADICIONAMOS O OPENPYXL AQUI NESTA LINHA:
      run: python -m pip install --upgrade pip pyinstaller openpyxl
      
    - name: Compilar o .exe
      run: pyinstaller --onefile main.py
      
    - name: Salvar o .exe para Download
      uses: actions/upload-artifact@v4
      with:
        name: Diagnostico-Erros-Excel
        path: dist/main.exe
